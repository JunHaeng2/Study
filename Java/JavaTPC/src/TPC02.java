/*
java의 경우는 class 파일이 exe 파일이 아니다.
JVM 이라고 불리우는 실행 엔진이 필요한거임

src -> 컴파일 하는 파일들이 들어있는 부분
컴파일 -> javac 이라는 명령어를 활용하여 컴파일
bin -> 컴파일 후 class들이 저장되는 부분

JVM(Java Virtual Machine)

1차 컴파일 (byte code) -> JVM이 OS에 맞게 재컴파일(실행코드)
-> exe code(실행) 

java는 어떤 os에 fix에서 사용하려고 만든 언어가 아니라, 어떤 운영체제든 상관없이
os에 독립적으로 구동할수있도록 만들어진것이기 때문에 이런 식의 동작상태를 갖는다.
*/ 

public class TPC02 {
    
}
